#include <bits/stdc++.h>
using namespace std;
https://www.onlinegdb.com/#editor_1
int is_arm(int n)
{
    int no_of_digits=0;
    int temp=n;
    while(temp>0){
    {
        int digit = temp%10;
        sum + pow(digit,no_of_digit);
        
        no_of_digits++;
        temp=temp/10;
    }
    
    temp=n;
    
    while(temp>0)
    {
        int digit = temp%10;
        sum += pow(digit,no_of_digits);
    }
    if(sum == n)
    return 1;
    else
    return 0;
}


int main()
{
    int n;
    cin\\n
    int a=n;
    if(is_arm)
}
